<div class="row">
  <div class="col-md-6">
    <table class="table table-condensed table-bordered ham-neighbors-counts">
      <thead>
      <tr>
      <th>State</th><th>Still trying</th><th>Successful</th><th>Not Found</th>
      </tr>
      </thead>
      <tbody>
      {% for state, values in state_counts %}

      <tr>
        <td>{{ state }}</td>
        <td>{{ values.0 }}</td>
        <td>{{ values.1 }}</td>
        <td>{{ values.2 }}</td>
      </tr>

      {% endfor %}

      <tr>
        <td><strong>Totals</strong></td>
        <td><strong>{{ totals.0 }}</strong></td>
        <td><strong>{{ totals.1 }}</strong></td>
        <td><strong>{{ totals.2 }}</strong></td>
      </tr>

      </tbody>
    </table>

    <p>Success rate: {{ '%.1f %%'|format(success_pc) }}</p>
  </div>
</div>
