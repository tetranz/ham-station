services:
  ham_station.commands:
    class: \Drupal\ham_station\Commands\HamStationCommands
    arguments: ['@ham_station.fcc_importer', '@ham_station.geocoder']
    tags:
      - { name: drush.command }
